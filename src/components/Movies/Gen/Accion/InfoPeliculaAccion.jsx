import React from 'react';

const InfoPeliculaAccion = ({ trailerAccion }) => {
    return (
        <div>
            {/* {trailerAccion.length > 0 && (
                <iframe
                    width="560"
                    height="315"
                    src={`https://www.youtube.com/embed/${trailerAccion[0].key}`}
                    title={trailerAccion[0].name}
                    frameBorder="0"
                    allowFullScreen
                ></iframe>
            )} */}
        </div>
    );
}

export default InfoPeliculaAccion;
